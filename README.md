# Arty A7-100T Real-Time Streaming Convolution Accelerator

This repository targets the Digilent Arty A7-100T (`xc7a100tcsg324-1`). Milestone 1 establishes a trustworthy board-debug foundation before camera, convolution, and Ethernet work begins.

## Milestone 1 interface

- `BTN0` is an asynchronous, active-high reset input with asynchronous assertion and two-clock synchronous release.
- `LD4` (`led[0]`) is a visible heartbeat generated by counter bit 26. It changes state every 0.671 seconds at 100 MHz.
- `LD5` through `LD7` mirror debounced switches `SW0` through `SW2`.
- All four switches are sampled into the UART status line.
- Pressing any of `BTN1` through `BTN3` requests an immediate status line.
- The USB-UART transmitter emits `M1 OK SW=0xN` after reset and every five seconds at 115200 baud, 8 data bits, no parity, and 1 stop bit.
- The USB-UART receive pin is exposed and synchronized, but receive functionality is reserved for a later milestone.

## Build and simulation

Run these commands from the `scripts/` directory in a Vivado command prompt:

```text
vivado -mode batch -source create_project.tcl
vivado -mode batch -source run_simulations.tcl
vivado -mode batch -source build_bitstream.tcl
```

If Vivado reports a Tcl App Store/user-cache path error on Windows, set this for the current PowerShell before launching it:

```powershell
$env:XILINX_LOCAL_USER_DATA = "NO"
```

The build script writes:

```text
docs/timing_summary_milestone1.rpt
docs/utilization_milestone1.rpt
```

Monitor the board's serial output with:

```text
python scripts/python/serial_monitor.py --list
python scripts/python/serial_monitor.py --port COM5
python scripts/python/serial_monitor.py --port COM5 --output docs/uart_terminal_milestone1.txt
```

The monitor requires `pyserial` (`python -m pip install pyserial`). Replace `COM5` with the detected port.

## Status

- [ ] Milestone 1: Minimal board bring-up
  - [x] Heartbeat RTL and reset behavior
  - [x] Reset synchronizer
  - [x] Input synchronizing/debouncing
  - [x] Switches, extra buttons, LEDs, and USB-UART constraints
  - [x] 115200 8N1 transmitter and status-message generator
  - [x] Self-checking UART and top-level smoke testbenches pass in Vivado 2026.1 XSim
  - [x] Python serial monitor written and syntax-checked
  - [ ] Timing and utilization reports captured for this revision
  - [ ] Bitstream programmed and LED/reset behavior observed on hardware
  - [ ] Laptop-side UART text captured and confirmed readable at 115200 8N1
- [ ] Milestone 2: Core convolution datapath
- [ ] Milestone 3: Camera bring-up
- [ ] Milestone 4: Ethernet bring-up
- [ ] Milestone 5: Full integration

Milestone 1 deliberately remains unchecked until the bitstream, timing, and physical-board pass criteria are evidenced. See `docs/milestone1_logic_walkthrough.md` for a detailed implementation explanation and `docs/milestone1_hardware_validation.md` for the exact hardware checklist.
